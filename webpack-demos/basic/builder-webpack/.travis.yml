language: node_js

sudo: false

cache:
  apt: true
  directories:
    - node_modules

node_js: stable # 设置相应的版本

install:  # 安装项目依赖
  - npm install -D  # 安装构建器依赖
  - cd ./test/smoke/template
  - npm install -D  # 安装模板项目依赖
  - cd ../../../

scripts:
  - npm test
